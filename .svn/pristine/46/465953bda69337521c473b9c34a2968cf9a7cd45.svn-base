package kr.or.ddit.ddtown.service.live;

public interface LiveService {

	/**
	 * 구루미 API를 호출하여 라이브 룸을 생성하고 접속 URL을 반환합니다.
	 * @param memUsername 방송을 시작하는 유저 ID
	 * @param memNicknm 방송을 시작하는 유저의 닉네임
	 * @return 구루미 룸 적속 URL
	 */
	String createLiveRoom(String memUsername, String memNicknm);
}
